version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: raw_events
        columns:
          - name: event_id
            tests:
              - unique
              - not_null
          - name: occurred_at
            tests:
              - not_null
          - name: payload
            tests:
              - not_null
      - name: quarantine_events
