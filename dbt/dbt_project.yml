name: production_data_pipeline
version: 1.0.0
config-version: 2

profile: production_data_pipeline

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  production_data_pipeline:
    silver:
      +schema: silver
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
